// Test field access with actual offsets

struct Point {
    x: int32,
    y: int32
}

function test_field_access() -> int32 {
    let p: Point;
    p.x = 100;
    p.y = 200;
    return p.x + p.y;
}

function main() -> int32 {
    return test_field_access();
}